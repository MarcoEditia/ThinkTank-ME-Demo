version: '3.8'
services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      # Saves the SQLite database permanently to a folder named 'sqlite_data' on your computer
      - ./sqlite_data:/app/data
